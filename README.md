# reverse-string-in-c
The program reads a string using `fgets()` and reverses it using a loop without using any built-in string reverse functions. This approach is portable and commonly used in exams and beginner-level C programming.
# Reverse a String in C

This repository contains a simple C program to reverse a string entered by the user.

## 🛠️ Technologies Used
- C Programming
- Standard Input/Output Library (`stdio.h`)


